// Каталог сегментов рынка и ниш. Источник: docs/security-engine/01_Architecture_FINAL.md §2
// и 02_Segmentation_FINAL.md §1 (шаги 3-4) — при расхождении между файлами
// правила сегментации берутся из Файла 02 (см. Файл 01 §15, "единственный источник правды").
//
// hasNicheStep=false → сегмент ведёт сразу в лист ожидания (Файл 02: Розничная
// торговля / Общепит / Другое), без показа списка ниш.
// На нишу: paidAudit — есть ли готовый контент теста (34 вопроса). Тест
// бесплатен для всех ниш; paidAudit=false просто значит "контент ещё не
// готов" — кнопка ведёт на заглушку с листом ожидания (Файл 05).

const SEGMENTS = [
  {
    key: 'beauty',
    label: 'Красота и здоровье',
    hasNicheStep: true,
    // Единственный сегмент, где разрешён мульти-выбор ниш (все ниши с
    // paidAudit: true имеют готовый контент теста) — остальные сегменты
    // сознательно остаются одно-нишевыми, см. задачу про мультивыбор в
    // docs/security-engine.
    multiNiche: true,
    niches: [
      { key: 'manicure', label: 'Маникюр и педикюр', paidAudit: true },
      { key: 'lashes_brows', label: 'Ресницы и брови', paidAudit: true },
      { key: 'hair', label: 'Волосы (парикмахерские услуги)', paidAudit: true },
      { key: 'massage', label: 'Массаж (без медицинской лицензии)', paidAudit: true },
      { key: 'tattoo', label: 'Тату, пирсинг и перманентный макияж', paidAudit: true },
      { key: 'depilation', label: 'Депиляция (шугаринг, воск, нить)', paidAudit: true },
      { key: 'solarium', label: 'Солярий', paidAudit: true },
      // Барбершоп (19.08.2026) — та же нормативная база, что "Волосы"
      // (СП 2.1.3678-20 не выделяет барбершопы отдельно), контент —
      // адаптация hair.js без блока химических составов (не типично для
      // барбершопа), см. комментарий в content/violations/barbershop.js.
      { key: 'barbershop', label: 'Барбершоп', paidAudit: true },
    ],
  },
  {
    key: 'fitness',
    label: 'Фитнес и активность',
    hasNicheStep: true,
    // dance/yoga переключены на paidAudit: true 08.09.2026 — контент
    // (violations/paid-questions/pdf/mandatory-documents/pdf/attention-zones)
    // построен адаптацией fitness_gym.js по итогам research
    // law-compliance-monitor того же дня, см. шапку violations/dance.js и
    // violations/yoga.js про источники и что требует проверки юристом.
    // pilates добавлен 09.09.2026 — та же санитарная база (раздел VI СП
    // 2.1.3678-20), что и у dance/yoga, повторного чтения документа не
    // потребовалось; блок 3 (оборудование) адаптирован отдельно под
    // реформер/кадиллак (не скопирован ни с fitness_gym, ни с yoga буквально
    // — см. шапку violations/pilates.js).
    niches: [
      { key: 'fitness_gym', label: 'Фитнес-студия / тренажёрный зал', paidAudit: true },
      { key: 'dance', label: 'Танцы', paidAudit: true },
      { key: 'yoga', label: 'Йога / растяжка', paidAudit: true },
      { key: 'pilates', label: 'Пилатес', paidAudit: true },
      // martial_arts (09.09.2026) — та же санитарная база, что у остальных
      // ниш сегмента; "кроссфит" сознательно не выделен отдельно, см.
      // шапку violations/martial-arts.js — по составу риска не отличается
      // от fitness_gym.
      { key: 'martial_arts', label: 'Бокс и единоборства', paidAudit: true },
    ],
  },
  {
    key: 'cleaning',
    label: 'Клининг',
    hasNicheStep: true,
    // Один узкий ниша-пункт на старте (решение владельца) — спецклининг и
    // биоклининг сознательно отложены на отдельный заход из-за
    // концентрации юридической сложности именно там, см. комментарий в
    // начале violations/cleaning-basic.js.
    niches: [
      { key: 'cleaning_basic', label: 'Уборка помещений (жильё и офисы)', paidAudit: true },
    ],
  },
  {
    key: 'household',
    label: 'Бытовые услуги',
    hasNicheStep: true,
    // Ателье (10.09.2026) и ремонт обуви (10.09.2026, тот же день) — обе
    // ниши используют один и тот же правовой каркас (бытовой подряд, ГК РФ
    // §2 гл.37), см. комментарии в шапках violations/atelier.js и
    // violations/shoe-repair.js. Фотостудии, химчистки — другие типичные
    // "бытовые услуги" по классификации Правил бытового обслуживания
    // населения (ПП РФ №1514) — сознательно отложены на отдельный заход.
    niches: [
      { key: 'atelier', label: 'Ателье (пошив и ремонт одежды)', paidAudit: true },
      { key: 'shoe_repair', label: 'Ремонт обуви', paidAudit: true },
    ],
  },
  {
    key: 'retail',
    label: 'Розничная торговля',
    // Фаза C плана (30.08.2026): раньше hasNicheStep=false вёл сразу в лист
    // ожидания без вопросов — теперь ниша 'universal' (общий тест: 152-ФЗ,
    // кассы, пожарная безопасность при наличии помещения, кадровый блок при
    // сотрудниках — то, что одинаково для любого бизнеса, не только
    // отраслевые нарушения розницы, которых здесь пока нет). Тот же ключ
    // 'universal', что и у сегмента 'other' ниже — контент один на двоих,
    // сегмент влияет только на подпись/сообщения, не на содержание теста.
    // paidAudit: true с 30.08.2026 — контент написан по итогам
    // research-прохода law-compliance-monitor того же дня (11 вопросов, 4
    // блока: регистрация/уведомление, 152-ФЗ, пожарная безопасность при
    // наличии помещения, кадровый блок при сотрудниках). Часть штрафов
    // честно помечена "требует проверки юристом" прямо в
    // content/violations/universal.js — источники по некоторым суммам
    // расходились между собой, не выдаём их за окончательные.
    hasNicheStep: true,
    niches: [{ key: 'universal', label: 'Общие требования (без отраслевой специфики)', paidAudit: true }],
  },
  {
    key: 'food',
    label: 'Общепит',
    hasNicheStep: true,
    // Узкая ниша на старте (решение владельца, 29.08.2026, по образцу
    // клининга): обычное кафе/кофейня/столовая, готовит и продаёт на месте.
    // Сознательно отложены — продажа алкоголя (171-ФЗ, отдельная лицензия и
    // ЕГАИС), детское/социальное питание (тендеры 44-ФЗ/223-ФЗ), логистика
    // через агрегаторы доставки, собственное производство-цех — см.
    // комментарий в начале violations/cafe-basic.js.
    // paidAudit: false — контент ещё не полон. Ключевой санитарный документ
    // (СанПиН 2.3/2.4.3590-20) заменяется на СП 2.3/2.4.4282-26 01.09.2026,
    // текст изменений не был опубликован на момент разведки (28.08.2026) —
    // блок 2 (санитария) сознательно оставлен тонким, дозаполняется после
    // 01.09.2026, после чего paidAudit переключается на true.
    niches: [
      { key: 'cafe_basic', label: 'Кафе, кофейня, столовая (без алкоголя)', paidAudit: false },
    ],
  },
  {
    key: 'other',
    label: 'Другое',
    // См. комментарий у 'retail' выше — тот же универсальный слой, тот же
    // ключ ниши 'universal', тот же контент, просто под другой подписью.
    hasNicheStep: true,
    niches: [{ key: 'universal', label: 'Общие требования (без отраслевой специфики)', paidAudit: true }],
  },
];

function findSegment(segmentKey) {
  return SEGMENTS.find((s) => s.key === segmentKey) || null;
}

function findNiche(segmentKey, nicheKey) {
  const segment = findSegment(segmentKey);
  if (!segment) return null;
  return segment.niches.find((n) => n.key === nicheKey) || null;
}

module.exports = { SEGMENTS, findSegment, findNiche };
