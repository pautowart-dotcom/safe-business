-- "+ Пришло" раньше всегда прибавлял ровно 1 — неудобно для расходников,
-- которые считаются в мл/г (пришла баночка геля 500 мл, а не "1 штука").
-- container_size — необязательный объём/количество одной тары, которым
-- предзаполняется количество при нажатии "Пришло" (frontend/src/pages/
-- Supplies.jsx) — само число всё равно можно поменять перед подтверждением.
ALTER TABLE supplies ADD COLUMN IF NOT EXISTS container_size NUMERIC(10,2);
