-- Владелец сам решает, сколько расходника (гель-лак и т.п.) в среднем
-- уходит на одного клиента — необязательное поле. Если задано, форма
-- нового визита (frontend/src/pages/Visits.jsx) сама подставляет эту
-- позицию с этим количеством, не дожидаясь ручного выбора мастером;
-- ручной выбор расходника из списка остаётся рабочим как раньше, для
-- всего, чему норма не задана, или сверх нормы.
ALTER TABLE supplies ADD COLUMN IF NOT EXISTS default_quantity_per_visit NUMERIC(10,2);
