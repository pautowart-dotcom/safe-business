-- Способ оплаты визита (План 04.08.2026, п.3) — необязательное поле, вводит
-- тот же человек, что и сам визит (мастер/админ/владелец, без requireRole на
-- visits.routes.js), значение просто фиксируется на визите. Сводная разбивка
-- выручки по способам оплаты за период — отдельная, более чувствительная
-- вещь (реальная выручка включая наличные, не видна налоговой) и показывается
-- только владельцу, см. finance/summary.routes.js.
ALTER TABLE visits ADD COLUMN IF NOT EXISTS payment_method VARCHAR(20)
    CHECK (payment_method IN ('cash', 'card', 'transfer', 'other'));
