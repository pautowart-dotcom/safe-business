-- Ниша визита (обсуждение 08.08.2026) — опционально, для студий с
-- несколькими нишами ("Красота и здоровье"): позволяет видеть разбивку
-- выручки по нише в Финансах, вместо одной общей суммы. Не FK — ключи ниш
-- валидируются на уровне приложения (visits.routes.js), тем же способом,
-- что security_profile_niches.niche.
ALTER TABLE visits ADD COLUMN IF NOT EXISTS niche VARCHAR(50);
