-- Анонимный разовый аудит (19.08.2026, п.5 плана — переделка по явному
-- уточнению владельца: не покупка внутри уже зарегистрированного аккаунта,
-- а отдельный публичный продукт на лендинге БЕЗ формы регистрации, только
-- email). Технически — та же схема users/companies/memberships, что у
-- обычной регистрации (полное переиспользование движка теста/скоринга/PDF/
-- разовой оплаты, см. 0091), просто создаётся автоматически в момент начала
-- анонимного теста, с плейсхолдер-почтой вместо формы. is_guest отличает
-- такие записи: (а) разрешает checkout-one-time обновить email этого
-- пользователя на реальный при оплате, (б) вебхуку — понять, что нужно
-- отправить результат на почту и ссылку "установить пароль", а не просто
-- пуш владельцу платформы.
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_guest BOOLEAN NOT NULL DEFAULT false;
