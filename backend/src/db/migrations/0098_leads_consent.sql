-- Согласие на обработку персональных данных для публичной формы заявок
-- (21.08.2026, владелец: "берём телефон и имя, а галочки согласия нет —
-- юридический косяк"). До этой миграции sales_leads.phone/name собирались
-- вообще без явного согласия субъекта данных — реальный пробел по 152-ФЗ,
-- не формальность. NULL допускается для заявок, созданных ДО этой миграции
-- (владельцем вручную через форму в приложении, не с публичной ссылки —
-- там согласие не требуется, это не сбор данных третьего лица).
ALTER TABLE sales_leads ADD COLUMN IF NOT EXISTS personal_data_consent_at TIMESTAMPTZ;
