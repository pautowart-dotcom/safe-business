После деплоя Пакета 4 не получается войти в приложение — POST /api/auth/login
возвращает 401 (в логах nginx это видно, запрос доходит до бэкенда). Логин и
пароль точно верные (в старой кэшированной сессии в браузере вход остаётся
рабочим, проблема только при новой попытке входа).

Пожалуйста, разберись:
1. Проверь код обработчика /api/auth/login — не менялся ли он в рамках Пакета 4
   (сравни с git log/diff).
2. Проверь, не затронули ли миграции Пакета 4 таблицу пользователей/companies
   так, что сравнение пароля или поиск пользователя теперь работает не так,
   как раньше.
3. Если нужно — добавь временное отладочное логирование в этот обработчик
   (по аналогии с существующим [invite-debug]), задеплой, дай мне попробовать
   войти ещё раз, и покажи, что покажут логи в этот момент.

Не переделывай остальной функционал, ищи именно причину этой конкретной ошибки.